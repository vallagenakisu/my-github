#include <bits/stdc++.h>
#define f(i, n) for (int i = 0; i < n; i++)
using namespace std;
void sol()
{
    // int x, y, n;
    // cin >> x >> y >> n;
    
    // int f = (n - y) / x;
    // int res = f * x + y;
    // cout << res << endl;
    cout << __gcd(1,10);
}
int main()
{
    int t;
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cin >> t;
    while (t--)
    {
        sol();
    }
}
