#include <bits/stdc++.h>
#define f(i, n) for (int i = 0; i < n; i++)
using namespace std;
void sol()
{
    int n, m, k;
    cin >> n >> m >> k;
    int a[n], b[n];
    f(i, n) cin >> a[i];
    f(i, n) cin >> b[i];
    
}
int main()
{
    int t;
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cin >> t;
    while (t--)
    {
        sol();
    }
}
